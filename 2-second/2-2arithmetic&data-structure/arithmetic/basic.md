# 五、加密算法
加密算法分类：单向加密（Hash算法）、对称加密和非对称加密。

## 5.1 单向加密
特征：不可逆、输入一样，输出必然相同、定长输出。  
补充：会有散列冲突（hash冲突）问题，常用于信息传输完整性校验（验签）。  
常见算法：MD5、SHA、CRC-32

## 5.2 对称加密
特征：加密和解密的密钥一致，不安全。  
常见算法：DES、3DES、AES

## 5.3 非对称加密
特征：加密和解密的密钥不一致，安全。  
RSA、ECC、DSA

## 补充知识
### （1）初始化向量IV
打到语义安全，让攻击者难以对同一把密钥的密文进行破解
java中的实现javax.crypto.spec.IvParameterSpec，
[百度百科](https://baike.baidu.com/item/%E5%88%9D%E5%A7%8B%E5%8C%96%E5%90%91%E9%87%8F/22660836?fr=aladdin)



# 七、一致性哈希
（1）0~ (2^32)-1的数字组成一个环链；
（2）节点通过哈希计算后分布在环链上；
（3）要匹配值时，将key执行哈希计算，根据计算后的值在环链上顺时针找到最近的节点。
（4）为了分布均匀，避免哈希倾斜，添加了虚拟节点；既在环链中创建虚拟节点，虚拟
节点和真实节点进行映射。

一致性哈希的优点：分布均匀，移除、新增节点不影响其它节点。

# 八、正则表达式
正则表达式是基于回溯的，消耗CPU资源。
